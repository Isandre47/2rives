{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Emission{% endblock %}

{% block body %}
    <div class="container">
        <h1>Editer une émission</h1>
        <div class="row">
            <div class="col-10 offset-1">
                {{ include('emission/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
            </div>
        </div>
        <div class="row justify-content-around">

            <button ><a href="{{ path('emission_index') }}">Retour à la liste</a></button>

            {{ include('emission/_delete_form.html.twig') }}
        </div>

    </div>

{% endblock %}
